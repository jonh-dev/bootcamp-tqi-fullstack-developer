// Manipulando um arquivo

// var preco = 2;

var desconto = 0.2;
const PRECO = 2;

var total = PRECO - desconto;

preco = preco - desconto;

// Constante não muda o valor. 

// Variavel muda o valor.

// ---------------------------------------------------------------------------------------

// Função

function soma(a, b){
    // Para conferir se está funcionando é usado console.log(a + b);
    return a + b; // 8
}

soma(3, 5);